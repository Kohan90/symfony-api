nelmio_api_doc:
    documentation:
        info:
            title: Symfony API
            description: OTP generation and validation API!
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
